# Valheim 게임 데이터 추가 작업 리뷰

> 기존 마인크래프트 전용으로 동작하던 RAG 챗봇을 **다중 게임(Multi-Game) 지원 구조**로 확장하면서, 두 번째 게임으로 **발헤임(Valheim)** 데이터를 통합한 작업의 기술 리뷰.

---

## 1. 작업 배경 및 목표

마인크래프트 RAG가 어느 정도 안정화된 후, 같은 파이프라인을 다른 게임에 재사용할 수 있는지 검증하기 위해 발헤임을 두 번째 도메인으로 선정했다. 핵심 요구사항은 다음과 같다.

* **데이터 격리**: 마인크래프트와 발헤임 문서가 서로의 검색에 섞여 들어가지 않을 것.
* **소스 다양화**: 단일 소스의 정보 부족 한계를 극복하기 위해 **공식형 자료 + 커뮤니티 자료**를 모두 수집할 것.
* **출처 신뢰도 차등화**: 검증된 위키와 유저 경험담을 LLM이 구분하여 답변할 수 있을 것.
* **UI 일관성**: 같은 챗봇 인터페이스에서 게임만 전환하면 즉시 해당 게임 전문가로 동작할 것.

---

## 2. 데이터 수집 (Crawling)

발헤임 데이터는 **2단계 크롤링 전략**으로 수집했다. 1차로 정리된 자료(위키)를, 2차로 실전 노하우(커뮤니티)를 보강하는 구조다.

### 2-1. 1차 수집: 발헤임 나무위키 (`valheim_namuwiki_loader.py`)

* **시작점**: `https://namu.wiki/w/Valheim`
* **수집 방식**: BFS 기반 재귀 크롤링 (`max_depth=1`)
* **링크 필터**: `/w/Valheim` 또는 `/w/%EB%B0%9C%ED%97%A4%EC%9E%84`(URL 인코딩된 "발헤임")으로 시작하는 내부 링크만 따라감 → 다른 게임 문서로 새지 않도록 제어
* **차단 방지**: 요청마다 `time.sleep(3)` + 실제 브라우저 User-Agent 위장
* **방문 추적**: `visited_urls` set으로 동일 페이지 중복 요청 방지
* **노이즈 제거**: BeautifulSoup으로 `<script>`, `<style>`, `<nav>`, `<header>`, `<footer>` 태그 사전 제거 후 본문 텍스트만 추출

### 2-2. 2차 수집: 디시인사이드 발헤임 갤러리 공략글 (`valheim_dcinside_loader.py`)

나무위키만으로는 "보스 빠르게 잡는 빌드", "지하실 열쇠 없이 들어가는 법" 같은 **실전 플레이 노하우**를 다 담을 수 없어서 추가했다.

#### 타겟 선정의 핵심: URL 파라미터로 양질의 데이터만 필터링

```
https://gall.dcinside.com/mgallery/board/lists/?id=valheim
    &exception_mode=recommend   # 개념글(커뮤니티 추천 글)만
    &search_head=40             # '공략' 카테고리만
```

* `exception_mode=recommend`: 디시 자체 추천 시스템을 통과한 글 = **노이즈/잡담 1차 필터링 완료**
* `search_head=40`: 글머리표가 '공략'인 글만 = **창작 자랑/일반 잡담 제외**
* 두 조건을 합치니 4년치 누적 글 중 **딱 31개**만 통과 → 양은 적지만 밀도가 매우 높은 큐레이션 데이터셋이 됨

#### 크롤러 설계 시 주요 의사결정

| 이슈 | 해결 |
| --- | --- |
| 디시는 봇 차단이 매우 엄격 | User-Agent + Accept-Language + Referer 헤더를 실제 브라우저처럼 구성, 요청 간격 **5초** |
| 페이지네이션 무한루프 위험 | 새 페이지의 글 ID가 직전과 동일하면 자동 종료 (`seen_post_ids` 비교) |
| 이미지로만 정보 전달하는 글 (예: "음식 조합 가이드"가 표 이미지) | 본문 텍스트 길이가 100자 미만이면 RAG 부적합으로 판단해 자동 스킵 |
| 중간 중단/재개 필요 | `processed_dc_posts.txt`에 처리 완료한 글 번호를 한 줄씩 기록 → 재실행 시 이어받기 |
| 파싱 실패해도 다시 시도하지 않게 | 본문이 짧아서 스킵된 글도 처리 완료 목록에 기록 |

#### 본문 추출 셀렉터

* 리스트 페이지: `tr.us-post`에서 `.gall_num`(글번호) + `.gall_tit a`(제목) 추출, `.gall_subject`가 '공지/AD'인 행은 제외
* 글 페이지: `.write_div`에서 본문만 추출 → `<script>`, `<style>`, `<iframe>` 제거 후 텍스트화

> 사전 검증 단계에서 DCInside HTML 구조가 가정대로인지 짧은 테스트 스크립트로 먼저 확인한 뒤 본 크롤러를 돌려서 시간 낭비를 막았다.

---

## 3. 청킹 전략 (Chunking)

마인크래프트와 동일한 전략을 사용해 일관성을 유지했다.

* **분할기**: `RecursiveCharacterTextSplitter`
* **청크 크기**: 1000자
* **오버랩**: 200자 (청크 경계에서 문맥 끊김 방지)
* **분할 우선순위**: 단락(`\n\n`) → 줄바꿈(`\n`) → 문장(마침표) → 단어 → 문자 순으로 가능한 한 의미 단위 보존

### 출처 머리말(Source Prefix) 주입 — 이번 작업의 핵심 설계

각 청크의 본문 가장 위에 **출처를 명시한 한 줄을 붙여서** 임베딩한 점이 중요하다.

```python
# 나무위키
content = f"출처: {title}\n내용: {chunk}"

# 디시인사이드
content = f"출처: 디시인사이드 발헤임 갤러리 공략 - {title}\n내용: {chunk}"
```

이렇게 한 이유:

1. **임베딩 품질 향상**: "출처: 엘더" 같은 머리말이 청크 내용과 함께 벡터화되어, 의미 검색 시 키워드 매칭 보조 신호로 작동.
2. **LLM의 출처 인지**: 검색된 Context가 LLM에게 전달될 때, LLM이 본문 첫 줄만 읽어도 **이 정보가 공식 위키인지 유저 공략인지** 즉시 알 수 있음. → 별도 메타데이터 파싱 로직 불필요.
3. **답변 시 인용(Citation)**: LLM이 답변 본문에 `(유저 공략글 "..." 참고)`처럼 자연스럽게 인용을 박을 수 있음.

---

## 4. 임베딩 전략 (Embedding)

### 4-1. 임베딩 모델

* **모델**: `jhgan/ko-sroberta-multitask` (HuggingFace)
* **선정 이유**:
  * 한국어 의미 유사도 태스크에 특화된 SBERT 계열 모델
  * 다양한 한국어 NLU 태스크로 멀티태스크 학습되어 도메인 일반화 성능 좋음
  * 마인크래프트와 동일 모델 사용 → 추후 게임 간 임베딩 비교/혼합 실험 시 일관성 확보
* **설정**:
  * `device='cpu'`: 로컬 개발 환경에서 GPU 없이 동작
  * `normalize_embeddings=True`: 코사인 유사도 검색 정확도 향상

### 4-2. Vector DB 구조 — 컬렉션 분리 전략

ChromaDB 인스턴스는 하나만 두고, **게임별로 컬렉션(collection)을 분리**했다.

```
chroma_db/
├── chroma.sqlite3
├── (마인크래프트 컬렉션 - default)
└── (valheim 컬렉션)
```

```python
# 발헤임 로더는 명시적으로 valheim 컬렉션 지정
vectorstore = Chroma(
    persist_directory="./chroma_db",
    embedding_function=embeddings,
    collection_name="valheim"
)
```

이 구조의 장점:

* **검색 도메인 격리**: 발헤임 모드에서 마인크래프트 문서가 절대 검색되지 않음. 메타데이터 필터링 방식보다 빠르고 안전.
* **DB 파일은 공유**: 임베딩 모델 로딩, sqlite 인덱스 등 인프라 비용은 한 번만.
* **확장성**: 다음 게임(예: Stardew Valley)을 추가할 때 새 컬렉션만 추가하면 끝.

### 4-3. 메타데이터 설계

같은 발헤임 컬렉션 안에서도 **출처별 신뢰도 구분**을 위해 메타데이터를 차등화했다.

| 필드 | 나무위키 | 디시인사이드 |
| --- | --- | --- |
| `game` | `valheim` | `valheim` |
| `source` | `namuwiki` | `dcinside` |
| `type` | `발헤임위키` | `발헤임커뮤니티공략` |
| `item` | 문서 제목 | 글 제목 |
| `post_id` | (없음) | 디시 글 번호 |

`post_id`를 디시 데이터에 추가한 이유는, 추후 답변에 디시 글이 인용됐을 때 **원문 링크로 바로 점프시키는 기능**(`https://gall.dcinside.com/mgallery/board/view/?id=valheim&no={post_id}`)을 만들 수 있도록 미리 깔아둔 것.

---

## 5. 챗봇 통합 (`app.py`)

### 5-1. 다중 게임 설정 (`GAME_CONFIG`)

게임별 메타데이터를 하나의 딕셔너리로 통합 관리. 사이드바 드롭다운 선택 시 즉시 해당 게임 모드로 전환.

```python
GAME_CONFIG = {
    "마인크래프트": {"icon": ..., "collection": None, "accent": "#3CAF55", ...},
    "발헤임":     {"icon": ..., "collection": "valheim", "accent": "#E07B3C", ...}
}
```

* 게임 전환 시 `st.session_state.messages`를 비워서 **이전 게임의 대화 맥락이 새 게임 답변에 영향 주지 않도록** 처리.
* 각 게임 고유 액센트 컬러를 헤더 카드, 사이드바 버튼 호버, 채팅 입력창 테두리에 적용 → 시각적으로 어느 게임 모드인지 즉각 인지 가능.

### 5-2. 출처 인지 프롬프트 (Source-Aware Prompt) — 가장 중요한 통합 설계

청킹 단계에서 본문 머리말에 박아둔 "출처: ..." 표시를 **프롬프트가 명시적으로 활용**하도록 설계했다.

핵심 가이드라인 두 가지:

1. **디시 공략 활용 시 자동 면책 박스**
   * Context에 디시인사이드 글이 포함되어 있으면, 답변 맨 위에 다음 문구를 자동으로 붙임:
     > 💡 이 답변에는 디시인사이드 발헤임 갤러리 유저들의 실전 공략이 포함되어 있습니다. 공식 정보가 아니므로 게임 버전이나 상황에 따라 다를 수 있습니다.
   * 본문에서 인용 시 `(유저 공략글 "글제목" 참고)` 형태로 출처 명시

2. **Context에 아예 없는 내용 답변 시 면책 조항**
   * 일반 지식 기반 답변임을 분명히 알리는 ⚠️ 박스 부착

이 설계로 사용자는 **답변의 출처와 신뢰도를 한눈에 구분**할 수 있고, RAG 평가 지표 중 **Faithfulness(사실 부합성)** 와 **Citation(출처 표기)** 을 동시에 충족.

---

## 6. 회고 및 한계점

### 잘된 점

* **2단계 수집 전략**으로 위키성 정보(스펙, 메커니즘)와 경험성 정보(빌드, 공략)를 모두 확보. 공식 위키만 썼을 때보다 답변 다양성과 실용성이 크게 향상됨.
* **수집 단계의 사전 큐레이션** (개념글 + 공략 카테고리 필터)으로 LLM이 봐야 할 노이즈를 원천적으로 줄여 RAG 품질 향상.
* **출처 머리말을 청크 본문에 박는 방식**이 임베딩 검색과 LLM 인용을 모두 자연스럽게 만들어줌. 메타데이터 필터링이나 별도 후처리 없이 프롬프트만으로 출처 분기 처리 가능.
* **컬렉션 분리 + GAME_CONFIG 패턴**으로 다른 게임 추가가 매우 단순해짐. 신규 게임 추가 시 [로더 1개 + 설정 한 줄]이면 끝.

### 한계 및 개선 여지

* **이미지 텍스트 손실**: 디시 공략에는 표/이미지로만 표현된 핵심 정보(예: "음식 조합 가이드")가 있는데, 본문이 짧다고 스킵 처리해버려 이런 글들의 가치를 활용하지 못함. → **OCR(예: PaddleOCR, Tesseract)** 도입으로 이미지 속 텍스트까지 흡수하면 데이터 풍부도 향상 가능.
* **글 작성 시기에 따른 정보 노후화**: 디시 글은 2021~2025년에 걸쳐 작성됨. 발헤임은 패치가 잦아서 옛날 글의 수치/메커니즘이 현재와 다를 수 있음. 현재는 면책 조항으로만 대응 중. → **메타데이터에 작성일을 추가**하고 LLM에게 "최신 글 우선 참고" 지시를 추가하면 더 좋음.
* **검색 품질의 정량 측정 부재**: 현재는 체감으로 답변 품질을 평가 중. → **RAGAS** 같은 평가 프레임워크로 발헤임 컬렉션에 대해서도 Context Relevance / Faithfulness / Answer Relevance를 정량 측정해볼 필요.
* **소스 가중치 미적용**: 모든 검색 결과에 동일 가중치를 적용 중. 실제로는 "수치 질문은 위키 우선, 전략 질문은 디시 우선" 같은 가중치 조정이 가능. → 향후 **Re-ranking 모델**(예: bge-reranker) 도입 시 출처별 가중치 적용 검토.

---

## 7. 한 줄 결론

> **단일 도메인 RAG를 멀티 도메인으로 확장할 때, 핵심은 "데이터 분리(컬렉션) + 출처 머리말 주입 + 출처 인지 프롬프트"의 3단 콤보다.** 이 패턴으로 발헤임을 추가하는 데 들인 시간은 마인크래프트 초기 구축의 1/5 수준이었으며, 같은 패턴을 다른 게임에도 그대로 재사용 가능하다.
